/// <reference types="express" />
import { Request, Response, Route } from '@teambit/express';
import type { Logger } from '@teambit/logger';
import { ElementsMain } from './elements.main.runtime';
declare type UrlParams = {
    /** `/elements/:elementPath(*)` */
    elementsPath?: string;
};
export declare class ElementsRoute implements Route {
    /**
     * elements extension.
     */
    private elements;
    private logger;
    constructor(
    /**
     * elements extension.
     */
    elements: ElementsMain, logger: Logger);
    route: string;
    method: string;
    middlewares: ((req: Request<UrlParams>, res: Response) => Promise<import("express").Response<any, Record<string, any>>>)[];
}
export {};
