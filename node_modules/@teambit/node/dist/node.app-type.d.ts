import { Logger } from '@teambit/logger';
import { ApplicationType } from '@teambit/application';
import { ReactEnv } from '@teambit/react';
import { NodeEnv } from './node.env';
import { NodeApp } from './node.application';
import type { NodeAppOptions } from './node-app-options';
export declare class NodeAppType implements ApplicationType<NodeAppOptions> {
    readonly name: string;
    private nodeEnv;
    private logger;
    constructor(name: string, nodeEnv: NodeEnv & ReactEnv, logger: Logger);
    createApp(options: NodeAppOptions): NodeApp;
}
