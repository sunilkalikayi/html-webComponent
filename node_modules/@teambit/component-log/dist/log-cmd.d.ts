import { Command, CommandOptions } from '@teambit/cli';
import { ComponentLogMain } from './component-log.main.runtime';
export default class LogCmd implements Command {
    private componentLog;
    name: string;
    description: string;
    extendedDescription: string;
    group: string;
    alias: string;
    options: CommandOptions;
    migration: boolean;
    remoteOp: boolean;
    skipWorkspace: boolean;
    arguments: {
        name: string;
        description: string;
    }[];
    constructor(componentLog: ComponentLogMain, docsDomain: string);
    report([id]: [string], { remote, parents }: {
        remote: boolean;
        parents: boolean;
    }): Promise<string>;
    json([id]: [string], { remote, parents }: {
        remote: boolean;
        parents: boolean;
    }): Promise<string[] | import("@teambit/legacy/scopes/component/legacy-component-log").LegacyComponentLog[]>;
}
