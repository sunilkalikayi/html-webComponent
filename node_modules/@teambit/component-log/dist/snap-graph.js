"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.buildSnapGraph = buildSnapGraph;
function _graph() {
  const data = require("@teambit/graph.cleargraph");
  _graph = function () {
    return data;
  };
  return data;
}
function buildSnapGraph(componentLog) {
  const graph = new (_graph().Graph)();
  // add nodes
  componentLog.forEach(snap => {
    graph.setNode(new (_graph().Node)(snap.hash, snap));
  });
  // add edges
  componentLog.forEach(snap => {
    snap.parents.forEach(parent => {
      graph.setEdge(new (_graph().Edge)(snap.hash, parent, 'parent'));
    });
  });
  return graph;
}

//# sourceMappingURL=snap-graph.js.map