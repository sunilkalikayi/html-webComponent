export declare class BitBaseEvent<T> {
    readonly type: string;
    readonly version: string;
    readonly timestamp: number;
    readonly data: T;
    constructor(type: string, version: string, timestamp: number, data: T);
}
