import { Command, CommandOptions } from '@teambit/cli';
import { CheckoutMain } from './checkout.main.runtime';
export declare class CheckoutCmd implements Command {
    private checkout;
    name: string;
    arguments: {
        name: string;
        description: string;
    }[];
    description: string;
    group: string;
    extendedDescription: string;
    alias: string;
    options: CommandOptions;
    loader: boolean;
    constructor(checkout: CheckoutMain);
    report([to, componentPattern]: [string, string], { interactiveMerge, ours, theirs, manual, reset, all, verbose, skipNpmInstall, skipDependencyInstallation, }: {
        interactiveMerge?: boolean;
        ours?: boolean;
        theirs?: boolean;
        manual?: boolean;
        reset?: boolean;
        all?: boolean;
        verbose?: boolean;
        skipNpmInstall?: boolean;
        skipDependencyInstallation?: boolean;
    }): Promise<string>;
}
