import { Command, CommandOptions } from '@teambit/cli';
import { ScopeMain } from '@teambit/scope';
import { BuilderMain } from '../builder.main.runtime';
export declare type ArtifactsOpts = {
    aspect?: string;
    task?: string;
    files?: string;
    outDir?: string;
};
export declare class ArtifactsCmd implements Command {
    private builder;
    private scope;
    name: string;
    description: string;
    extendedDescription: string;
    arguments: {
        name: string;
        description: string;
    }[];
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(builder: BuilderMain, scope: ScopeMain);
    report([componentPattern]: [string], artifactsOpts: ArtifactsOpts): Promise<string>;
}
