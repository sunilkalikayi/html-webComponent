/// <reference types="react" />
import { FileIconSlot } from '@teambit/code';
import { Section } from '@teambit/component';
export declare class CodeCompareSection implements Section {
    private fileIconSlot;
    constructor(fileIconSlot: FileIconSlot);
    navigationLink: {
        href: string;
        children: string;
        order: number;
    };
    route: {
        path: string;
        element: JSX.Element;
    };
}
