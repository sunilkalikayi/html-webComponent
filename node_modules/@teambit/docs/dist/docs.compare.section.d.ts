/// <reference types="react" />
import { Section } from '@teambit/component';
import { TitleBadgeSlot } from './overview';
export declare class OverviewCompareSection implements Section {
    private titleBadgeSlot;
    constructor(titleBadgeSlot: TitleBadgeSlot);
    navigationLink: {
        href: string;
        children: string;
        exact: boolean;
        order: number;
    };
    route: {
        path: string;
        element: JSX.Element;
    };
}
