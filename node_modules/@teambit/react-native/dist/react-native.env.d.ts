import { AspectMain } from '@teambit/aspect';
import { Bundler, BundlerContext } from '@teambit/bundler';
import { Environment, DependenciesEnv, PreviewEnv } from '@teambit/envs';
import type { ReactMain } from '@teambit/react';
import { WebpackConfigTransformer } from '@teambit/webpack';
export declare const ReactNativeEnvType = "react-native";
export declare class ReactNativeEnv implements Environment, DependenciesEnv, PreviewEnv {
    private react;
    private aspect;
    constructor(react: ReactMain, aspect: AspectMain);
    getHostDependencies(): Promise<string[]>;
    getTemplateBundler(context: BundlerContext, transformers?: WebpackConfigTransformer[]): Promise<Bundler>;
    createTemplateWebpackBundler(context: BundlerContext, transformers?: WebpackConfigTransformer[]): Promise<Bundler>;
    getDependencies(): {
        dependencies: {
            react: string;
            'react-dom': string;
            'react-native': string;
        };
        devDependencies: {
            react: string;
            'react-dom': string;
            'react-native': string;
            '@types/jest': string;
            '@types/react': string;
            '@types/react-dom': string;
            '@types/react-native': string;
            '@babel/runtime': string;
            '@types/testing-library__jest-dom': string;
        };
        peerDependencies: {
            react: string;
            'react-dom': string;
            'react-native': string;
            'react-native-web': string;
        };
    };
    __getDescriptor(): Promise<{
        type: string;
    }>;
}
