"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.reactNativeAlias = reactNativeAlias;
function _lodash() {
  const data = require("lodash");
  _lodash = function () {
    return data;
  };
  return data;
}
/**
 * set the alias of react-native$ to point to the react-native-web
 * @param config
 * @returns
 */
function reactNativeAlias(config) {
  const reactNativeWebPath = (0, _lodash().get)(config.raw, 'resolve.alias.react-native-web', require.resolve('react-native-web'));
  const newAliases = {
    'react-native$': reactNativeWebPath
  };
  config.removeAliases(['react-native']);
  config.addAliases(newAliases);
  return config;
}

//# sourceMappingURL=react-native-alias.js.map