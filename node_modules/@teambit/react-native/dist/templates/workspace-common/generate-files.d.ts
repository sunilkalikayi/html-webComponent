import { WorkspaceContext } from '@teambit/generator';
declare type GeneratedFile = {
    relativePath: string;
    content: string;
};
export declare function generateFiles(context: WorkspaceContext): Promise<GeneratedFile[]>;
export {};
