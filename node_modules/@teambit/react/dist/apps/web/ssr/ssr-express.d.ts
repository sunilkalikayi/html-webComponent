import { Asset } from '@teambit/bundler';
import { Logger } from '@teambit/logger';
import type { HtmlAssets } from '@teambit/react.rendering.ssr';
declare type ExpressSsrOptions = {
    name: string;
    workdir: string;
    port: number;
    app: any;
    assets: HtmlAssets;
    logger?: Logger;
};
export declare function createExpressSsr({ name, workdir, port, app, assets, logger }: ExpressSsrOptions): import("express-serve-static-core").Express;
export declare function loadSsrApp(workdir: string, appName: string): Promise<any>;
export declare function parseAssets(assets: Asset[], publicPath?: string): HtmlAssets;
export {};
