"use strict";

function _componentId() {
  const data = require("@teambit/component-id");
  _componentId = function () {
    return data;
  };
  return data;
}
function _indexFile() {
  const data = require("./index-file");
  _indexFile = function () {
    return data;
  };
  return data;
}
// react hook convention is `useX` (lowercase "use")
// it is required to enable hook-specific linting rules
const useFoobarSnapshot = `export { useFooBar } from './use-foo-bar';
`; // trailing line break

describe('templates: react-hook, index-file', () => {
  it('should match snapshot', () => {
    const ctx = {
      name: 'use-foo-bar',
      namePascalCase: 'UseFooBar',
      nameCamelCase: 'useFooBar',
      componentId: _componentId().ComponentID.fromString('baz.qux/use/foo/bar')
    };
    const result = (0, _indexFile().indexFile)(ctx);
    expect(result.content).toEqual(useFoobarSnapshot);
  });
});

//# sourceMappingURL=index-file.spec.js.map