import { PackageManagerClients } from '../consumer/config/legacy-workspace-config-interface';
export declare type PackageManagerResults = {
    stdout: string;
    stderr: string;
};
declare type installArgs = {
    modules?: string[] | {
        [key: string]: number | string;
    };
    packageManager?: PackageManagerClients;
    packageManagerArgs?: string[];
    packageManagerProcessOptions?: Record<string, any>;
    useWorkspaces?: boolean;
    dirs: string[];
    rootDir: string | null | undefined;
    installRootPackageJson: boolean;
    installPeerDependencies: boolean;
    installProdPackagesOnly?: boolean;
    verbose: boolean;
};
declare function getPeerDepsFromNpmList(npmList: string, packageManager: string): Promise<Record<string, any>>;
declare function getNpmVersion(): Promise<string | null | undefined>;
declare function getYarnVersion(): Promise<string | null | undefined>;
/**
 * a situation where rootDir and subDir have package.json, some of the packages may be shared
 * and some may be conflicted. And the "npm/yarn install" is done from the root dir.
 * package managers install the shared packages only once in the rootDir.
 * however, as to the conflicted packages, only npm@5 and above install it in the subDir.
 * others, install it in the root, which, result in an incorrect package resolution for the subDir.
 */
declare function isSupportedInstallationOfSubDirFromRoot(packageManager: string): Promise<boolean>;
declare function getPackageLatestVersion(packageName: string): Promise<string | null | undefined>;
declare const _default: {
    install: ({ modules, packageManager, packageManagerArgs, packageManagerProcessOptions, useWorkspaces, dirs, rootDir, installRootPackageJson, installPeerDependencies, installProdPackagesOnly, verbose, }: installArgs) => Promise<PackageManagerResults | PackageManagerResults[]>;
    printResults: ({ stdout, stderr }: {
        stdout: string;
        stderr: string;
    }) => void;
    isSupportedInstallationOfSubDirFromRoot: typeof isSupportedInstallationOfSubDirFromRoot;
    getNpmVersion: typeof getNpmVersion;
    getYarnVersion: typeof getYarnVersion;
    getPeerDepsFromNpmList: typeof getPeerDepsFromNpmList;
    getPackageLatestVersion: typeof getPackageLatestVersion;
};
export default _default;
