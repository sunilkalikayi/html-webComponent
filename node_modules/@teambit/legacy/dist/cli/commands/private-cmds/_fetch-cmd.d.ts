import { FETCH_TYPE } from '../../../api/scope/lib/fetch';
import { ObjectList } from '../../../scope/objects/object-list';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Fetch implements LegacyCommand {
    name: string;
    private: boolean;
    internal: boolean;
    description: string;
    alias: string;
    opts: CommandOptions;
    action([path, args]: [string, string], { type, noDependencies, includeArtifacts, }: {
        type: FETCH_TYPE;
        noDependencies: boolean;
        includeArtifacts: boolean;
    }): Promise<ObjectList>;
    report(objectList: ObjectList): string;
}
