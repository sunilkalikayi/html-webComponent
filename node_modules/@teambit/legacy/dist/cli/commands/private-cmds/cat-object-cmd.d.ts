import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class CatObject implements LegacyCommand {
    name: string;
    description: string;
    private: boolean;
    loader: boolean;
    alias: string;
    opts: CommandOptions;
    action([hash]: [string], { pretty, stringify, headers }: {
        pretty: boolean;
        stringify: boolean;
        headers: boolean;
    }): Promise<any>;
    report(file: any): string;
}
