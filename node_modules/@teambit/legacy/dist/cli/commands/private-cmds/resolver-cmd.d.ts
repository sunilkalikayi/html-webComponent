/**
 * this command is not in-use currently. consider deleting it.
 */
import { LegacyCommand } from '../../legacy-command';
declare class ResolverSet implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    private: boolean;
    loader: boolean;
    opts: never[];
    action([resolverPath]: [string]): Promise<any>;
    report(resolverPath: any): string;
}
declare class ResolverReset implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    action(): Promise<any>;
    report(): string;
}
export default class Resolver implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    commands: (ResolverSet | ResolverReset)[];
    private: boolean;
    action(): Promise<any>;
    report(resovlerPath: string): string;
}
export {};
