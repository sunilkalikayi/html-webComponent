import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Login implements LegacyCommand {
    name: string;
    description: string;
    group: Group;
    alias: string;
    skipWorkspace: boolean;
    opts: CommandOptions;
    action([]: Iterable<any>, // eslint-disable-line no-empty-pattern
    { port, suppressBrowserLaunch, npmrcPath, skipRegistryConfig, machineName, }: {
        port: string;
        suppressBrowserLaunch?: boolean;
        npmrcPath: string;
        skipRegistryConfig: boolean;
        machineName?: string;
    }): Promise<any>;
    report({ isAlreadyLoggedIn, username, npmrcPath, skipRegistryConfig, writeToNpmrcError, }: {
        isAlreadyLoggedIn: boolean;
        username: string;
        npmrcPath: string;
        skipRegistryConfig: boolean;
        writeToNpmrcError: boolean;
    }): string;
}
