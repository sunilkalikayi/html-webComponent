import { AddActionResults } from '../../../consumer/component-ops/add-components/add-components';
import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Add implements LegacyCommand {
    name: string;
    description: string;
    group: Group;
    extendedDescription: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    action([paths]: [string[]], { id, main, namespace, scope, override, }: {
        id: string | null | undefined;
        main: string | null | undefined;
        namespace: string | null | undefined;
        scope?: string;
        override: boolean;
    }): Promise<any>;
    splitList(val: string): string[];
    report({ addedComponents, warnings }: AddActionResults): string;
}
