import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
declare class RemoteAdd implements LegacyCommand {
    name: string;
    description: string;
    extendedDescription: string;
    alias: string;
    opts: CommandOptions;
    action([url]: [string], { global }: {
        global: boolean;
    }): Promise<any>;
    report({ name, host }: {
        name: string;
        host: string;
    }): string;
}
declare class RemoteRm implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    action([name]: [string], { global }: {
        global: boolean;
    }): Promise<any>;
    report(name: string): string;
}
export default class Remote implements LegacyCommand {
    name: string;
    description: string;
    group: Group;
    extendedDescription: string;
    alias: string;
    opts: CommandOptions;
    migration: boolean;
    commands: (RemoteAdd | RemoteRm)[];
    action(args: string[], { global }: {
        global: boolean;
    }): Promise<any>;
    report(remotes: {
        [key: string]: string;
    }): string;
}
export {};
