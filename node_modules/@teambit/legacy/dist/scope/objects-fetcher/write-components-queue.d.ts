import PQueue from 'p-queue';
export declare class WriteComponentsQueue {
    private queue;
    private processedIds;
    constructor(concurrency?: number);
    addComponent(id: string, fn: () => Promise<void>): Promise<Promise<void>>;
    getQueue(): PQueue<import("p-queue/dist/priority-queue").default, import("p-queue").DefaultAddOptions>;
    add<T>(fn: () => T, priority?: number): Promise<T>;
    onIdle(): Promise<void>;
}
