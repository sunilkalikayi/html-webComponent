import { BitError } from '@teambit/bit-error';
export default class ServerIsBusy extends BitError {
    queueSize: number;
    currentExportId: string;
    code: number;
    constructor(queueSize: number, currentExportId: string);
}
