"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.PersistFailed = void 0;
function _bitError() {
  const data = require("@teambit/bit-error");
  _bitError = function () {
    return data;
  };
  return data;
}
class PersistFailed extends _bitError().BitError {
  constructor(failedScopes, errors) {
    super(`failed persisting to the following scopes: ${failedScopes.join(', ')}
errors:
${Object.keys(errors).map(scopeName => `${scopeName} - ${errors[scopeName]}`).join('\n')}

the remote bit.dev will try to re-persist the data for the failed scopes.
   `);
  }
}
exports.PersistFailed = PersistFailed;