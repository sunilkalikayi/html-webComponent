import { BitIds } from '../../bit-id';
import { Consumer } from '../../consumer';
import Component from '../../consumer/component/consumer-component';
export declare function getAutoTagPending(consumer: Consumer, changedComponents: BitIds): Promise<Component[]>;
export declare type AutoTagResult = {
    component: Component;
    triggeredBy: BitIds;
};
export declare function getAutoTagInfo(consumer: Consumer, changedComponents: BitIds): Promise<AutoTagResult[]>;
