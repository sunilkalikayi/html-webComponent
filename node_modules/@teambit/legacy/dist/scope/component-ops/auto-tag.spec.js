"use strict";

require("core-js/modules/es.promise.js");
function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _autoTag() {
  const data = require("./auto-tag");
  _autoTag = function () {
    return data;
  };
  return data;
}
describe('AutoTag', () => {
  describe('getAutoTagPending', () => {
    it('should return an empty array when there are no components in the workspace', async () => {
      const consumer = {
        bitMap: {
          getAllBitIds: () => []
        },
        loadComponents: () => Promise.resolve({
          components: []
        })
      };
      // @ts-ignore AUTO-ADDED-AFTER-MIGRATION-PLEASE-FIX!
      const test = await (0, _autoTag().getAutoTagPending)(consumer, []);
      (0, _chai().expect)(test).to.be.an('array').and.empty;
    });
  });
});