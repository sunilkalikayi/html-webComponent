import { BitIds } from '../../bit-id';
import Consumer from '../../consumer/consumer';
import { Lane } from '../models';
import RemovedObjects from '../removed-components';
import Scope from '../scope';
/**
 * remove components from the model.
 *
 * previously, this class also removed dependencies from the scope, see https://github.com/teambit/bit/pull/5380 for
 * more details.
 */
export default class RemoveModelComponents {
    scope: Scope;
    bitIds: BitIds;
    force: boolean;
    consumer: Consumer | null | undefined;
    currentLane?: Lane | null;
    fromLane?: boolean;
    constructor(scope: Scope, bitIds: BitIds, force: boolean, consumer?: Consumer, currentLane?: Lane | null, fromLane?: boolean);
    remove(): Promise<RemovedObjects>;
    private getRemoveSingleData;
    private getDataForRemovingComponent;
}
