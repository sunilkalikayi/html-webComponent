import { Network } from './network';
import { SSHConnectionStrategyName } from './ssh/ssh';
export default function connect(host: string, name: string, strategiesNames?: SSHConnectionStrategyName[], localScopeName?: string): Promise<Network>;
