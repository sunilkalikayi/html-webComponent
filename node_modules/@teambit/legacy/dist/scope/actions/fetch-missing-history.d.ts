import { Scope } from '..';
import { Action } from './action';
declare type Options = {
    ids: string[];
};
/**
 * for lanes, when components from other scopes are exported to this scope, some history might be missing.
 */
export declare class FetchMissingHistory implements Action<Options> {
    execute(scope: Scope, options: Options): Promise<void>;
}
export {};
