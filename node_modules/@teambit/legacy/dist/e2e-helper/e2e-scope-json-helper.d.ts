import ScopesData from './e2e-scopes';
export default class ScopeJsonHelper {
    scopes: ScopesData;
    constructor(scopes: ScopesData);
    read(scopeJsonDir?: string): any;
    write(scopeJson: Record<string, any>, scopeJsonDir?: string): void;
    addKeyVal(key: string, val: any, scopeJsonDir?: string): void;
}
