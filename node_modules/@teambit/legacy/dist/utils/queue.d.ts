export default class Queue {
    private concurrency;
    private autoStart;
    private queue;
    constructor(concurrency?: number, autoStart?: boolean);
    addAll(fns: Array<() => any>): Promise<any[]>;
    add(fn: () => any, priority: number): Promise<any>;
    pause(): void;
    size(): number;
    onEmpty(): Promise<void>;
    onIdle(): Promise<void>;
}
