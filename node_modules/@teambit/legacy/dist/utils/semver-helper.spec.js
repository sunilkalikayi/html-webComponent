"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _semverHelper() {
  const data = require("./semver-helper");
  _semverHelper = function () {
    return data;
  };
  return data;
}
describe('semver-helper', () => {
  describe('getValidVersionOrReleaseType', () => {
    it('should recognize "patch" as releaseType', () => {
      const results = (0, _semverHelper().getValidVersionOrReleaseType)('patch');
      (0, _chai().expect)(results.releaseType).to.equal('patch');
      (0, _chai().expect)(results.exactVersion).to.be.undefined;
    });
    it('should recognize "1.0.0" as exactVersion', () => {
      const results = (0, _semverHelper().getValidVersionOrReleaseType)('1.0.0');
      (0, _chai().expect)(results.exactVersion).to.equal('1.0.0');
      (0, _chai().expect)(results.releaseType).to.be.undefined;
    });
  });
});