"use strict";

function _chai() {
  const data = require("chai");
  _chai = function () {
    return data;
  };
  return data;
}
function _resolvePkgNameByPath() {
  const data = require("./resolve-pkg-name-by-path");
  _resolvePkgNameByPath = function () {
    return data;
  };
  return data;
}
describe('resolvePackageNameByPath', () => {
  it('should return the correct package for non-scoped package', () => {
    (0, _chai().expect)((0, _resolvePkgNameByPath().resolvePackageNameByPath)('lodash/internal/file')).to.equal('lodash');
  });
  it('should return the correct package for scoped package', () => {
    (0, _chai().expect)((0, _resolvePkgNameByPath().resolvePackageNameByPath)('@angular/core/src/utils.ts')).to.equal('@angular/core');
  });
  it('should return the correct package for webpack sass-loader path (with tilda) and scoped package', () => {
    (0, _chai().expect)((0, _resolvePkgNameByPath().resolvePackageNameByPath)('~@teambit/base-ui.theme.colors/colors.module.scss')).to.equal('@teambit/base-ui.theme.colors');
  });
});