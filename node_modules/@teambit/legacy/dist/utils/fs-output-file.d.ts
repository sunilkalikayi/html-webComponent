export declare type OutputFileParams = {
    filePath: string;
    content: string;
    writeAutoGeneratedMessage?: boolean;
    override?: boolean;
    prefixMessage?: string;
};
export default function outputFile({ filePath, content, writeAutoGeneratedMessage, override, prefixMessage, }: OutputFileParams): Promise<string>;
