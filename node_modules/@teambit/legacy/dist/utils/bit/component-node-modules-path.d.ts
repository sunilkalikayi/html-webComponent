import BitId from '../../bit-id/bit-id';
import { ExtensionDataList } from '../../consumer/config/extension-data';
import { PathOsBasedRelative } from '../path';
export default function getNodeModulesPathOfComponent({ bindingPrefix, id, allowNonScope, defaultScope, // if an id doesn't have a scope, use defaultScope if exists. applies only when allowNonScope is true
extensions, isDependency, }: {
    bindingPrefix?: string;
    id: BitId;
    allowNonScope?: boolean;
    defaultScope?: string | null;
    extensions: ExtensionDataList;
    isDependency?: boolean;
}): PathOsBasedRelative;
