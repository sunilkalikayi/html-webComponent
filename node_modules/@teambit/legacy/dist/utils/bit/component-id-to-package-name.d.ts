import BitId from '../../bit-id/bit-id';
import { ExtensionDataList } from '../../consumer/config/extension-data';
/**
 * convert a component name to a valid npm package name
 * e.g. BitId { scope: util, name: is-string } => @bit/util.is-string
 */
export default function componentIdToPackageName({ id, bindingPrefix, defaultScope, withPrefix, extensions, isDependency, }: {
    id: BitId;
    bindingPrefix: string | null | undefined;
    defaultScope?: string | null;
    withPrefix?: boolean;
    extensions: ExtensionDataList;
    isDependency?: boolean;
}): string;
