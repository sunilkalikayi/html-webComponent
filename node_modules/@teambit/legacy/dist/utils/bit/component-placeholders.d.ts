/**
 * search for placeholders, such as {main}, {name} in a template and replace them with the values
 * from the component with some manipulations
 */
export declare function replacePlaceHolderForPackageValue({ mainFile, name, scope, scopeId, owner, }: {
    mainFile?: string | null;
    name: string;
    scope?: string | null;
    scopeId?: string | null;
    owner?: string | null;
}, template: string): string;
