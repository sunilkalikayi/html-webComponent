"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
require("core-js/modules/es.promise.js");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = writeFile;
function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));
  _fsExtra = function () {
    return data;
  };
  return data;
}
function _os() {
  const data = require("os");
  _os = function () {
    return data;
  };
  return data;
}
async function writeFile(path, contents, options = {}) {
  await _fsExtra().default.outputFile(path, contents);
  if (options.gid || options.uid) {
    const user = (0, _os().userInfo)();
    await _fsExtra().default.chown(path, options.uid || user.uid, options.gid || user.gid);
  }
}