declare type Options = {
    emptyArrayMsg: string;
    keysColor: string;
    dashColor: string;
    numberColor: string;
    defaultIndentation: number;
    noColor: boolean;
    noAlign: boolean;
    inlineArrays?: any;
    stringColor: any;
};
declare function render(data: any, options?: Options, indentation?: number): string;
declare function renderString(data: any, options: any, indentation: any): string;
export { render, renderString };
