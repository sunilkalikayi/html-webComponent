import { ObjectList } from '../../../scope/objects/object-list';
export declare type ComponentObjectsInput = {
    path: string;
    objectList: string | ObjectList;
};
export declare type PushOptions = {
    clientId?: string;
    persist?: boolean;
};
export default function put({ path, objectList }: ComponentObjectsInput, pushOptions: PushOptions, headers?: Record<string, any>): Promise<string[]>;
