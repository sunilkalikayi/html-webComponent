import { BitError } from '@teambit/bit-error';
export declare class LaneNotFound extends BitError {
    scopeName: string;
    laneName: string;
    code: number;
    constructor(scopeName: string, laneName: string);
}
