/// <reference types="node" />
import { Readable } from 'stream';
export declare type FETCH_TYPE = 'component' | 'lane' | 'object' | 'component-delta';
export declare type FETCH_OPTIONS = {
    type: FETCH_TYPE;
    withoutDependencies: boolean;
    includeArtifacts: boolean;
    allowExternal: boolean;
    laneId?: string;
    onlyIfBuilt?: boolean;
    ignoreMissingHead?: boolean;
};
export default function fetch(path: string, ids: string[], // ids type are determined by the fetchOptions.type
fetchOptions: FETCH_OPTIONS, headers?: Record<string, any> | null | undefined): Promise<Readable>;
