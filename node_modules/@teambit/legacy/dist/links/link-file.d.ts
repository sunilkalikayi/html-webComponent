import { BitId } from '../bit-id';
import { AbstractVinyl } from '../consumer/component/sources';
import { PathOsBased } from '../utils/path';
export default class LinkFile extends AbstractVinyl {
    override: boolean;
    ignorePreviousSymlink: boolean;
    writeAutoGeneratedMessage: boolean | null | undefined;
    srcPath: string | null | undefined;
    componentId: BitId | null | undefined;
    write(): Promise<string>;
    static load({ filePath, base, content, override, ignorePreviousSymlink, writeAutoGeneratedMessage, srcPath, componentId, }: {
        filePath: PathOsBased;
        base?: string;
        content: string;
        override?: boolean;
        ignorePreviousSymlink?: boolean;
        writeAutoGeneratedMessage?: boolean;
        srcPath?: string;
        componentId?: BitId;
    }): LinkFile;
}
