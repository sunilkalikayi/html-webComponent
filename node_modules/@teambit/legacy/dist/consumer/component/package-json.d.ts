import { BitId } from '../../bit-id';
import ConsumerComponent from '.';
export declare type PackageJsonProps = {
    name?: string;
    version?: string;
    homepage?: string;
    main?: string;
    dependencies?: Record<string, any>;
    devDependencies?: Record<string, any>;
    peerDependencies?: Record<string, any>;
    license?: string;
    scripts?: Record<string, any>;
    workspaces?: string[];
    private?: boolean;
    componentId?: BitId;
    exported?: boolean;
};
export default class PackageJson {
    name?: string;
    version?: string;
    homepage?: string;
    main?: string;
    dependencies?: Record<string, any>;
    devDependencies?: Record<string, any>;
    peerDependencies?: Record<string, any>;
    componentRootFolder?: string;
    license?: string;
    scripts?: Record<string, any>;
    workspaces?: string[];
    componentId?: BitId;
    exported?: boolean;
    constructor(componentRootFolder: string, { name, version, homepage, main, dependencies, devDependencies, peerDependencies, license, scripts, workspaces, componentId, exported, }: PackageJsonProps);
    static loadSync(componentRootFolder: string): PackageJson | null;
    static hasExisting(componentRootFolder: string): boolean;
    static getPackageJson(pathStr: string): Promise<any>;
    static saveRawObject(pathStr: string, obj: Record<string, any>): Promise<void>;
    static removeComponentsFromWorkspaces(rootDir: string, pathsTOoRemove: string[]): Promise<void>;
    static removeComponentsFromDependencies(rootDir: string, components: ConsumerComponent[]): Promise<void>;
    static extractWorkspacesPackages(packageJson: {
        [k: string]: any;
    }): string[] | null;
    static updateWorkspacesPackages(packageJson: any, workspacesPackages: any): void;
    /**
     * according to Yarn Git repo, the workspaces type configured as the following
     * `workspaces?: Array<string> | WorkspacesConfig`
     * and `WorkspacesConfig` is:
     * `export type WorkspacesConfig = { packages?: Array<string>, nohoist?: Array<string> };`
     * see https://github.com/yarnpkg/yarn/blob/master/src/types.js
     */
    static throwForInvalidWorkspacesConfig(packageJson: any): void;
}
