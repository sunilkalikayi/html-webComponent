import { DependencyTreeParams, ImportSpecifier, DependenciesTree } from './types/dependency-tree-type';
export declare type LinkFile = {
    file: string;
    importSpecifiers: ImportSpecifier[];
};
/**
 * Function for fetching dependency tree of file or dir
 * @param baseDir working directory
 * @param workspacePath
 * @param filePaths path of the file to calculate the dependencies
 * @param bindingPrefix
 */
export declare function getDependencyTree({ componentDir, // component rootDir, for legacy-authored it's the same as workspacePath
workspacePath, filePaths, bindingPrefix, resolveModulesConfig, visited, cacheProjectAst, }: DependencyTreeParams): Promise<{
    tree: DependenciesTree;
}>;
