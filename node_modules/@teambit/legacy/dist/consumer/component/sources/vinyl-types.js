"use strict";

// taken from @types/vinyl@2.0.4, the FileConstructor has changed, see the comments inside it.
// without this change, typescript throws errors when trying to extend the Vinyl class due to the
// overloading.

let File;