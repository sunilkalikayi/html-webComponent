import Consumer from './consumer';
declare type LoadConsumerFunc = {
    (currentPath?: string, newInstance?: boolean): Promise<Consumer>;
    cache?: Record<string, Consumer>;
};
export declare const loadConsumer: LoadConsumerFunc;
export declare function loadConsumerIfExist(currentPath?: string, newInstance?: boolean): Promise<Consumer | undefined>;
export {};
