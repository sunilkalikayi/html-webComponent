"use strict";

require("core-js/modules/es.promise.js");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.FormatTask = void 0;
class FormatTask {
  constructor(aspectId, name = 'format') {
    this.aspectId = aspectId;
    this.name = name;
  }
  async execute(context) {
    const formatter = context.env.getFormatter();
    // TODO: add option to select between check and format here
    const results = await formatter.check(context);
    const componentsResults = results.results.map(formatResult => {
      return {
        component: formatResult.component,
        metadata: {
          results: formatResult.results
        },
        errors: []
      };
    });
    return {
      componentsResults
    };
  }
}
exports.FormatTask = FormatTask;

//# sourceMappingURL=format.task.js.map