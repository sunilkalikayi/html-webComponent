import { Command, CommandOptions } from '@teambit/cli';
import { SignMain } from './sign.main.runtime';
declare type SignOptions = {
    multiple: boolean;
    alwaysSucceed: boolean;
    push: boolean;
    lane?: string;
};
export declare class SignCmd implements Command {
    private signMain;
    name: string;
    private: boolean;
    description: string;
    extendedDescription: string;
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(signMain: SignMain);
    report([components]: [string[]], { multiple, alwaysSucceed, push, lane }: SignOptions): Promise<string | {
        data: string;
        code: number;
    }>;
    private warnForMissingVersions;
}
export {};
