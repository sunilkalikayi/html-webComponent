import { Command, CommandOptions } from '@teambit/cli';
import { ScopeMain } from '@teambit/scope';
import { Logger } from '@teambit/logger';
import { UpdateDependenciesMain, UpdateDepsOptions } from './update-dependencies.main.runtime';
export declare class UpdateDependenciesCmd implements Command {
    private updateDependenciesMain;
    private scope;
    private logger;
    name: string;
    private: boolean;
    description: string;
    extendedDescription: string;
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(updateDependenciesMain: UpdateDependenciesMain, scope: ScopeMain, logger: Logger);
    report([data]: [string], updateDepsOptions: UpdateDepsOptions): Promise<string>;
    private parseData;
}
