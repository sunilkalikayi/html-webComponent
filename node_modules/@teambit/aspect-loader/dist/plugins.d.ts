import { Component } from '@teambit/component';
import { Aspect } from '@teambit/harmony';
import { PluginDefinition } from './plugin-definition';
import { Plugin } from './plugin';
export declare type PluginMap = {
    [filePath: string]: PluginDefinition;
};
export declare class Plugins {
    readonly component: Component;
    readonly plugins: Plugin[];
    constructor(component: Component, plugins: Plugin[]);
    getByRuntime(runtime: string): Plugin[];
    load(runtime: string): Promise<Aspect>;
    has(): boolean;
    static from(component: Component, defs: PluginDefinition[], resolvePath?: (path: string) => string): Plugins;
}
