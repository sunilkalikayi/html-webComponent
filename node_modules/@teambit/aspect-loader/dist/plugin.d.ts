import { Aspect } from '@teambit/harmony';
import { PluginDefinition } from './plugin-definition';
export declare class Plugin {
    readonly def: PluginDefinition;
    readonly path: string;
    constructor(def: PluginDefinition, path: string);
    private _instance;
    /**
     * determines whether the plugin supports a certain runtime.
     */
    supportsRuntime(runtime: string): boolean;
    register(sourceAspect: Aspect): void;
    require(): unknown;
}
