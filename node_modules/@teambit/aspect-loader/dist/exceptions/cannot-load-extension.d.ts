import { BitError } from '@teambit/bit-error';
export declare class CannotLoadExtension extends BitError {
    private extensionName;
    private error;
    constructor(extensionName: string, error: Error);
    report(verbose?: boolean): string;
    toJson(): void;
}
