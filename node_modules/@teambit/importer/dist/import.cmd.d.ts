import { Command, CommandOptions } from '@teambit/cli';
import { MergeStrategy } from '@teambit/legacy/dist/consumer/versions-ops/merge-version/merge-version';
import { ImporterMain } from './importer.main.runtime';
export declare class ImportCmd implements Command {
    private importer;
    private docsDomain;
    name: string;
    description: string;
    arguments: {
        name: string;
        description: string;
    }[];
    extendedDescription: string;
    group: string;
    alias: string;
    options: CommandOptions;
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    _packageManagerArgs: string[];
    constructor(importer: ImporterMain, docsDomain: string);
    report([ids]: [string[]], { path, objects, displayDependencies, override, verbose, json, conf, skipNpmInstall, skipDependencyInstallation, merge, saveInLane, dependencies, dependents, allHistory, }: {
        path?: string;
        objects?: boolean;
        displayDependencies?: boolean;
        override?: boolean;
        verbose?: boolean;
        json?: boolean;
        conf?: string;
        skipNpmInstall?: boolean;
        skipDependencyInstallation?: boolean;
        merge?: MergeStrategy;
        saveInLane?: boolean;
        dependencies?: boolean;
        dependents?: boolean;
        allHistory?: boolean;
    }): Promise<any>;
}
