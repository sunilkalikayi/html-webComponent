import { ShowFragment, Component } from '@teambit/component';
import { DependencyResolverMain } from '../dependency-resolver.main.runtime';
export declare class DependenciesFragment implements ShowFragment {
    private depResolver;
    constructor(depResolver: DependencyResolverMain);
    renderDependencies(component: Component): Promise<string>;
    json(component: Component): Promise<{
        title: string;
        json: import("..").SerializedDependency[];
    }>;
    renderRow(component: Component): Promise<{
        title: string;
        content: string;
    }>;
}
