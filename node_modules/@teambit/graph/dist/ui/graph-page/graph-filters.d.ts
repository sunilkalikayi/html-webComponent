/// <reference types="react" />
import { CardProps } from '@teambit/base-ui.surfaces.card';
declare type GraphFilters = {
    isFiltered: boolean;
    onChangeFilter: (isFiltered: boolean) => void;
    disable?: boolean;
} & CardProps;
export declare function GraphFilters({ onChangeFilter, isFiltered, disable, ...rest }: GraphFilters): JSX.Element;
export {};
