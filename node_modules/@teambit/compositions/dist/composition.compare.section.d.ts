/// <reference types="react" />
import { EmptyStateSlot } from '@teambit/compositions';
import { Section } from '@teambit/component';
export declare class CompositionCompareSection implements Section {
    private emptyStateSlot;
    constructor(emptyStateSlot: EmptyStateSlot);
    navigationLink: {
        href: string;
        children: string;
        order: number;
    };
    route: {
        path: string;
        element: JSX.Element;
    };
}
