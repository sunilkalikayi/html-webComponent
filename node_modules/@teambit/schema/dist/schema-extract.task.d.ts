import { BuildTask, BuiltTaskResult } from '@teambit/builder';
export declare class SchemaExtract implements BuildTask {
    readonly aspectId: string;
    name: string;
    description: string;
    constructor(aspectId: string);
    execute(): Promise<BuiltTaskResult>;
}
