import { Command, CommandOptions } from '@teambit/cli';
import { ComponentMain } from '@teambit/component';
import { Logger } from '@teambit/logger';
import type { SchemaMain } from './schema.main.runtime';
export declare class SchemaCommand implements Command {
    private schema;
    private component;
    private logger;
    name: string;
    description: string;
    extendedDescription: string;
    group: string;
    options: CommandOptions;
    constructor(schema: SchemaMain, component: ComponentMain, logger: Logger);
    report([pattern]: [any]): Promise<string>;
    json([pattern]: [any]): Promise<Record<string, any>[]>;
    private getSchemas;
}
